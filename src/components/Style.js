import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    txtG:{
        fontSize: 28,
        fontWeight: 'bold',
        alignItems: 'center'
    },
    txtM:{
        fontSize: 20,
        fontWeight: '500',
        alignItems: 'center'
    }
})

export default styles